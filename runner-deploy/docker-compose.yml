version: '3'

services:
  runner:
    image: ghcr.io/gitnastics/actions-runner:latest
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.role == worker
    environment:
      - ACCESS_TOKEN=$ACCESS_TOKEN
      - ORGANIZATION=$ORGANIZATION